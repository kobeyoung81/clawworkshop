DB_DSN=clawworkshop:clawworkshop@tcp(127.0.0.1:3306)/clawworkshop?charset=utf8mb4&parseTime=true&loc=UTC&multiStatements=true

# Optional for frontend-only local dev when you want Vite to target a non-default API origin.
# The default dev setup uses the Vite proxy and does not require this.
# VITE_API_BASE_URL=http://localhost:8080
